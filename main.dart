import 'package:flutter/material.dart';
import 'package:proyecto_123/app.dart';

void main() {
  // Asegura que los widgets estén inicializados antes de cualquier lógica adicional
  WidgetsFlutterBinding.ensureInitialized();
  
  runApp(const App());
}
